services:
  frontend:
    build: ./front
    labels:
      # Enable Traefik
      - "traefik.enable=true"

      # HTTPS router
      - "traefik.http.routers.laferme-frontend.rule=Host(`website.santoriello.ch`)"
      - "traefik.http.routers.laferme-frontend.entrypoints=websecure"
      - "traefik.http.routers.laferme-frontend.tls.certresolver=le"

      # HTTP → HTTPS redirect router
      - "traefik.http.routers.laferme-frontend-http.rule=Host(`website.santoriello.ch`)"
      - "traefik.http.routers.laferme-frontend-http.entrypoints=web"
      - "traefik.http.routers.laferme-frontend-http.middlewares=redirect-to-https"

      # Middleware to redirect HTTP → HTTPS
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
